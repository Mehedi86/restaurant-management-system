import React from 'react';
import useDynamicTitle from '../hooks/useDynamicTitle';

const Menu = () => {
    useDynamicTitle('Menu')
    return (
        <div>
            
        </div>
    );
};

export default Menu;